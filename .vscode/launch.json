{
	"version": "0.2.0",
	"configurations": [
		{
			"type": "byond",
			"request": "launch",
			"name": "Launch DreamSeeker",
			"preLaunchTask": "Build All",
			"dmb": "${workspaceFolder}/${command:CurrentDMB}"
		},
		{
			"type": "byond",
			"request": "launch",
			"name": "Launch DreamDaemon",
			"preLaunchTask": "Build All",
			"dmb": "${workspaceFolder}/${command:CurrentDMB}",
			"dreamDaemon": true
		},
		{
			"type": "byond",
			"request": "launch",
			"name": "Launch DreamSeeker (No Build)",
			"preLaunchTask": "Wait for DreamDaemon",
			"dmb": "${workspaceFolder}/${command:CurrentDMB}"
		}
	],
	"compounds": [
		{
			"name": "Launch Both",
			"configurations": ["Launch DreamDaemon", "Launch DreamSeeker (No Build)"],
			"stopAll": true
		}
	]
}
