/obj/item/device/sugar_copier
	name = "sugar copier"
	desc = "It can create a sugar copy of an item, provided it has enough loaded sugar."

/*
/obj/item/device/afterattack(atom/target, mob/user, proximity_flag, click_parameters)
	. = ..()
	if(isitem(target))
		if(!isturf(I.loc))
		var/obj/item/I = target
		if(I.w_class < W_CLASS_HUGE)
*/
